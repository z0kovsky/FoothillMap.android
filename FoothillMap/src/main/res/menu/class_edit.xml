<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menu_edit_cancel"
        android:orderInCategory="1"
        android:showAsAction="ifRoom"
        android:icon="@drawable/ic_menu_edit_cancel"
        android:title="@string/menu_edit_cancel" />

    <item
        android:id="@+id/menu_edit_done"
        android:orderInCategory="2"
        android:showAsAction="always"
        android:icon="@drawable/ic_menu_edit_done"
        android:title="@string/menu_edit_done" />

</menu>
